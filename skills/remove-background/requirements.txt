# 供下载者本地安装运行（与仓库 backend/pyproject.toml 锁定相同主版本，避免网站与下载版用不同 rembg/onnxruntime）。
# 注意：rembg 2.x 依赖 pymatting -> numba -> llvmlite；pymatting 旧约束把 numba 锁在 <0.54，
# 连带 llvmlite 0.36 仅支持 Python <3.10，故 Python 3.11+ 无法直接安装。
# 下方显式拉 pymatting>=1.1.15 + numba>=0.60 + llvmlite>=0.43（与网站 uv override 等效），
# 让 rembg 能在 3.11+ 正常安装运行（alpha matting 后处理在网站中不使用，无影响）。
rembg>=2.0,<3
Pillow>=10
onnxruntime>=1.18,<2
numpy>=1.24
scipy>=1.10
scikit-image>=0.19
imageio>=2.27
requests>=2.28
numba>=0.60
llvmlite>=0.43
pymatting>=1.1.15
